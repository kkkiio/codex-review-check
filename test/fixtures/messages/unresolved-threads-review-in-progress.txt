=== annotation (core.setFailed — the line agents read via gh run view --log-failed) ===
2 unresolved Codex review thread(s) are blocking (listed above). A Codex review of the current HEAD is already in progress. Next steps: 1) resolve each handled conversation 2) gh run rerun 33499862152 --failed. If you already resolved them after this run started, no further action is needed — just re-run.

=== job log lines (core.info) ===
Blocking Codex review thread: https://github.com/owner/repo/pull/42#discussion_r1
Blocking Codex review thread: src/widget.ts (outdated)

=== job summary (web UI only) ===
# Codex Review

| Pull request | HEAD | State | Signal |
| --- | --- | --- | --- |
| [owner/repo#42](https://github.com/owner/repo/pull/42) | `821c759c9c3256e8a677800dbe8e4a88ae5e2ab0` | failure | unresolved-threads |

## Blocking Codex review threads

Resolve each handled GitHub review conversation, then re-run this check.

- [docs/adr/0012-glyph-width.md:18](https://github.com/owner/repo/pull/42#discussion_r1)
- src/widget.ts (outdated)

A Codex review of the current HEAD is already in progress, so no new review request is needed. Resolve the conversations above, then re-run the failed job:

```shell
gh run rerun 33499862152 --failed
```

If you already resolved the conversations after this run started, just re-run.
